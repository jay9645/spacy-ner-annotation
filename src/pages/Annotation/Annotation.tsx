import { Spin } from 'antd';
import React, { lazy, Suspense } from 'react';

// const AnnotationForm = lazy(() => import('./AnnotationForm'));

const Annotation = () => {
  return (
    <>
      {/* <Helmet title="Spacy NER Annotation App" />
      <br />
      <br />
      <CenteredContent>
        <br />
        <Suspense fallback={<Spin />}>
          <AnnotationForm />
        </Suspense>
      </CenteredContent>
    </> */}
    <h1>HELLO WORLD</h1>
    </>
  );
};

export default Annotation;
